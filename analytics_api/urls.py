from django.urls import path
# from .views import BlogViewsAnalyticsView, TopAnalyticsView, PerformanceAnalyticsView

urlpatterns = [
    # path('analytics/blog-views/', BlogViewsAnalyticsView.as_view()),
    # path('analytics/top/', TopAnalyticsView.as_view()),
    # path('analytics/performance/', PerformanceAnalyticsView.as_view()),
]
